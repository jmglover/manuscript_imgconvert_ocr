# TODO

- [ ] Add docker for Tesseract dependency esp windows users
- [ ] `--split-every N` option to output smaller searchable PDFs instead of
      one large one
- [ ] Embed a TTF in the PDF text layer so non-Latin scripts work

## Done

- [x] Add a JPEG conversion stage (`--format`) and default stored images to JPEG
