# Core dependencies for manuscript_ocr.py
# Install with:  pip install -r requirements.txt
pillow-heif>=1.4.0      # HEIC/HEIF decoding
Pillow>=12.0.0          # image handling
pytesseract>=0.3.13     # Tesseract OCR binding (default engine)
reportlab>=4.4.0        # searchable-PDF output

# NOTE: pytesseract is only a *binding*. You also need the Tesseract engine
# itself installed as a system program (it is not a pip package):
#   macOS:    brew install tesseract
#   Ubuntu:   sudo apt-get install tesseract-ocr
#   Windows:  https://github.com/UB-Mannheim/tesseract/wiki

# ---------------------------------------------------------------------------
# Optional, only if you use a non-default OCR engine. Uncomment as needed.
# ---------------------------------------------------------------------------
# google-cloud-vision>=3.15.0   # for --engine gvision (handwriting); also needs
#                               # a Google Cloud account + GOOGLE_APPLICATION_CREDENTIALS
# easyocr>=1.7.0                # for --engine easyocr (downloads model weights on first run)
# numpy>=1.26                   # required by easyocr
